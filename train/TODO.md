* reduce Docker image sizes, particularly `ic-train-worker`
* allow a worker to run multiple runs in parallel, if resources permit

# Optional
* use subprocess to easily start a development environment
